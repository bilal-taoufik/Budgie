services:
  nginx:
    ports:
      - "${APP_PORT:-80}:80"
      - "443:443"
    volumes:
      - app-public:/var/www/budgie/public:ro
      - app-storage:/var/www/budgie/storage:ro
      - ./docker/nginx/prod.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
